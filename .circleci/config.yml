version: 2

jobs:
  build:
    docker:
      - image: circleci/clojure:tools-deps-1.10.1.483
    steps:
      - checkout
      - restore_cache:
          keys:
            - clj-v1-{{ checksum "deps.edn" }}-{{ checksum "package-lock.json" }}
            - clj-v1-
      - run: npm install
      - run: |
          bin/kaocha \
            --plugin junit-xml \
            --junit-xml-file test-results/kaocha/$(date +"%s").xml
      - store_test_results:
          path: test-results
      - save_cache:
          key: deps-v1-{{ checksum "deps.edn" }}-{{ checksum "package-lock.json" }}
          paths:
            - ~/.m2
            - ~/.cljs/.aot_cache
            - ~/node_modules
            - ~/.gitlibs

